{
  "name": "rentflow-ai",
  "version": "1.0.0",
  "description": "AI-Powered Property Management on Arc with USDC",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "cd frontend && npm run build",
    "test": "npm run test:contracts && npm run test:backend",
    "test:contracts": "npx hardhat test",
    "test:backend": "cd backend && npm test",
    "deploy:contracts": "npx hardhat run scripts/deploy-contracts.ts --network arc",
    "deploy:db": "npx ts-node scripts/deploy-db.ts",
    "seed:db": "npx ts-node scripts/seed-data.ts",
    "setup:demo": "npx ts-node scripts/setup-demo-user.ts",
    "setup:passwords": "npx ts-node scripts/setup-passwords.ts",
    "debug:login": "npx ts-node scripts/debug-login.ts",
    "test:frontend-login": "npx ts-node scripts/test-frontend-login.ts",
    "check:user-mapping": "npx ts-node scripts/check-user-mapping.ts",
    "create:demo-lease": "npx ts-node scripts/create-demo-lease.ts",
    "debug:tenant-dashboard": "npx ts-node scripts/debug-tenant-dashboard.ts",
    "test:tenant-features": "npx ts-node scripts/test-tenant-features.ts",
    "check:user-exists": "npx ts-node scripts/check-user-exists.ts",
    "migrate:db": "npx ts-node scripts/run-migration.ts",
    "verify:migration": "npx ts-node scripts/verify-migration.ts",
    "verify:env": "npx ts-node scripts/verify-env.ts",
    "payment-scheduler": "npx ts-node backend/src/scripts/paymentSchedulerCron.ts",
    "setup:ai-analysis": "npx ts-node scripts/setup-ai-analysis.ts",
    "setup:voice-notifications": "npx ts-node scripts/setup-voice-notifications.ts",
    "setup:payment-scheduler": "npx ts-node scripts/setup-payment-scheduler.ts",
    "setup:blockchain-logging": "npx ts-node scripts/setup-blockchain-logging.ts",
    "setup:all-automation": "npx ts-node scripts/setup-all-automation.ts",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,sol,json,md}\""
  },
  "keywords": [
    "blockchain",
    "ai",
    "property-management",
    "arc",
    "usdc",
    "defi",
    "real-estate"
  ],
  "author": "RentFlow AI Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/uuid": "^10.0.0",
    "axios": "^1.12.2",
    "concurrently": "^8.2.2",
    "eslint": "^8.56.0",
    "hardhat": "^2.19.4",
    "prettier": "^3.1.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "@supabase/supabase-js": "^2.76.1",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}